import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.babyjat.app',
  appName: 'BabyJat',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
